<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp">

    <!-- TextView for the chat message -->
    <TextView
        android:id="@+id/messageText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|right"
        android:layout_weight="1"
        android:gravity="right"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:text="@string/newText"
        android:textSize="16sp" />

    <!-- ImageView for the incoming symbol -->
    <ImageView
        android:id="@+id/imageViewIncomingSymbol"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:contentDescription="@string/incoming_symbol_description"
        app:srcCompat="@android:drawable/sym_call_incoming" />

</LinearLayout>
